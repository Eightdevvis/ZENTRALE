# ZENTRALE – Python-Abhängigkeiten
# Installieren: venv/bin/pip install -r requirements.txt

# Web-Framework (Dashboard + REST API)
flask

# Tastatur-Simulation (Sensor-Simulation ohne echte Hardware, braucht sudo)
keyboard

# Spracherkennung (STT) – faster-whisper läuft auf CPU ohne GPU
faster-whisper

# Text-to-Speech (TTS) – offline
# sherpa-onnx liefert die chinesische Stimme (vits-zh-aishell3, Tutor-Modus).
# piper-tts liefert die deutsche Stimme (de_DE-kerstin-low, Haupt-Chat).
# Beide werden parallel im services/tts_service.py geladen – pro Sprache
# eine Engine. Modell-Download via services/download_tts_model.py.
sherpa-onnx
piper-tts

# Audio-Aufnahme und -Wiedergabe (Mikro fürs Persona-Zimmer / spätere Pi-Nutzung)
sounddevice
soundfile
# Voice-Activity-Detection fürs Immer-Zuhören im Tutor-Fenster (scripts/tutor_room.py).
# webrtcvad-wheels statt webrtcvad: liefert Wheels + kommt ohne pkg_resources aus
# (setuptools>=81 hat pkg_resources entfernt, klassisches webrtcvad importiert es).
webrtcvad-wheels

# HTTP-Client (für Whisper/TTS Services, falls nicht über stdlib)
requests

# Recurrence-Rules (iCal RRULE) für Routinen im Kalender-System
python-dateutil

# Natives Karten-Fenster (Maps-System): scripts/map_window.py rendert die Karte
# als zweites X11-Fenster mit echter antialiased Vektorgrafik (aus der TUI per
# 'w' aufklappbar). Nur dafür nötig — TUI/Backend laufen ohne. ~15 MB Wheel.
# numpy: vektorisierte Projektion der LOD-Geometrie (10m = >500k Punkte) — ohne
# das wären feine Zoomstufen sekundenlang pro Frame. Auch nur fürs Fenster.
pygame
numpy

# Verschlüsselter Zugangsdaten-Speicher fürs Mail-System (core/mail_secrets.py).
# Fernet/AES für data/mail_secrets.enc, entsperrt per Passphrase ZENTRALE_MAIL_KEY.
cryptography

# Optionaler OS-Keyring-Fallback für die Mail-Passphrase (interaktive Desktop-
# Session, Secret Service). Pure-Python, klein. Fehlt es (headless), greift
# einfach weiter nur ZENTRALE_MAIL_KEY. Siehe mail_secrets._passphrase().
secretstorage
jeepney

# Sprach-Tutor – Cloud-Leitung. Default-Provider 'qwen' (DashScope, Singapur,
# no-train) läuft über den OpenAI-/v1-kompatiblen Pfad (core/tutor_openai_compat.py)
# und braucht das openai-SDK. Deckt zugleich Mistral/DeepSeek/Groq/Gemini ab.
# Key kommt aus data/tutor_config.json (keys.DASHSCOPE_API_KEY), NICHT ins Repo.
# Der Offline-Default (Ollama) braucht das NICHT — nur der Cloud-Tutor.
openai
# anthropic-SDK (core/tutor_cloud.py) ist NUR für den teuren Claude-Verifikations-
# pfad (provider='claude') nötig. Erst installieren, wenn du den wirklich nutzt:
#   venv/bin/pip install anthropic
# anthropic
